# .github

GitHub Actions workflows are added in **Development Step 9** (`ci:` commits).

Until then this directory holds repo metadata only. Creating `.github/workflows/*` requires a GitHub token with the `workflow` scope (`gh auth refresh -s workflow`).
